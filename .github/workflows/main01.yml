name: Databricks_so_file_Deployment
on:
  push:
    branches: main
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v2
      
    - name: schmiddim-wget-unzip
      uses: schmiddim/action-wget-unzip@v2
      with:
        url: /source/velocity_modules.zip
        destination: /source
