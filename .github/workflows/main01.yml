name: Databricks_so_file_Deployment
on:
  push:
    branches: main
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v2
      
    - name: Just Extract
      uses: brunoborges/justextract@v1
      with:
        file: ./source/velocity_modules.zip
